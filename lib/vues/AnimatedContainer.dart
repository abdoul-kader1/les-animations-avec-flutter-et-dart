import 'package:flutter/material.dart';

class AnimatedContainers extends StatelessWidget{

  const AnimatedContainers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child:Text("Bienvenu dans le cours"),
      );
  }

}